# Kargo Warehouse, Stage, and Freight

Run `python3 startup.dev.py --offline --dry-run --step 11-install-kargo-dev.py` to validate the Kargo offline GitOps pipeline scaffolding.

Required footprint:

- `output/provisioned.yaml` → `provisioned.kargo.value`
