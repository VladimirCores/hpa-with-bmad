apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: agent-engine
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "7"
spec:
  project: default
  source:
    repoURL: http://10.6.0.1:9418/with-bmad.git
    targetRevision: HEAD
    path: gitops/agent-engine/rendered
  destination:
    server: https://kubernetes.default.svc
    namespace: hpdc-agents
  syncPolicy:
    automated:
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 15s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
