# Epic 4 Retrospective — Real-Time Telemetry Ingestion & Processing

- **Date:** 2026-08-11
- **Epic:** 4 — Real-Time Telemetry Ingestion & Processing
- **Stories:** 4-1 `build-iot-device-simulator-and-telemetry-acceptance-harness`, 4-2 `accept-telemetry-through-mqtt-http-and-grpc-routes` (story files exist, done); 4-3 `normalize-telemetry-into-protobuf-commonenvelope`, 4-4 `create-partitioned-pulsar-topics-for-telemetry`, 4-5 `apply-telemetry-back-pressure-and-drop-metric-controls`, 4-6 `process-telemetry-with-pulsar-functions-and-write-to-clickhouse`, 4-7 `create-clickhouse-device-metrics-tables-and-retention-policies`, 4-8 `cache-hot-device-state-and-alert-context-in-keydb`, 4-9 `process-event-telemetry-with-spin-wasm-functions`, 4-10 `validate-end-to-end-telemetry-pipeline-performance` (marked done in sprint-status, **no per-story story files** — delivered via consolidated offline GitOps scaffold under `gitops/epic4/`, later renamed to `gitops/platform/`)
- **Status:** Complete (10/10 in sprint-status; 2/10 with story records)
- **Participants:** Amelia (Developer, facilitating), John (Product Manager), Winston (System Architect), Murat (Test Architect), Master (Project Lead)
- **Previous retrospective:** None (first recorded retrospective for this epic)
- **Next epic:** Epic 3 (next retro in the 10→1 sweep)

## Epic Summary & Metrics

- **Completion:** 10/10 stories in sprint-status; 2/10 with story records (4-3..4-10 records missing)
- **Scope:** IoT device simulator (4-1), MQTT/HTTP/gRPC ingestion routes (4-2), CommonEnvelope normalization (4-3), partitioned Pulsar topics (4-4), back-pressure controls (4-5), Pulsar Functions→ClickHouse (4-6), ClickHouse tables + retention (4-7), KeyDB hot-state cache (4-8), Spin WASM event transforms (4-9), E2E pipeline validation (4-10)
- **Constraint honored:** offline/GitOps-safe — no internet, no live cluster
- **Record note:** Epic 4 definition lives at `### Epic 4` (h3) in epics.md while all other epics use `## Epic N` (h2)

## Successes

1. **Consolidated declarative scaffold.** Stories 4-3..4-10 were delivered through a single offline GitOps scaffold (`gitops/platform/base/platform-scaffold.yaml`) declaring the full substance: CommonEnvelope protobuf schema, 500ms aggregation window, 25,000-record ClickHouse batches, `device_metrics` with `ORDER BY (device_type, processed_timestamp)`, KeyDB cache with TTL, Spin WASM functions, back-pressure policy, and per-environment retention. One artifact, one source of truth.
2. **Verification stays green.** `tests/test_validate_platform_dev.py` passes offline ("Platform offline GitOps scaffold validation passed"), so the scaffold evidence is machine-checkable.
3. **Multi-protocol ingestion actually delivered.** The manifest declares HTTPRoute `/telemetry` (100K RPS rate-limit annotation), GRPCRoute, and TCPRoute (MQTT) backed by `pulsar-telemetry-ingestion`, plus per-device-type capacity config.

## Challenges & Growth Areas

1. **Thinnest story-record trail of any epic.** 4-3..4-10 have no story files; sprint-status says done but the audit trail lives only in the scaffold doc — which itself references pre-rename paths (`gitops/epic4/`, `29-install-epic4-dev.py`, `test_validate_epic4_dev.py`) that no longer exist after commit `c76fe6a`. Documentation drift on top of record drift.
2. **Task checklist drift in 4-2.** Story file shows unchecked tasks (2.3 MQTT, 2.4 gRPC, Task 3 Pulsar routing) despite `Status: done` — the manifest declares all of them, but the story file was never updated.
3. **Heading inconsistency.** `### Epic 4` (h3) broke machine discovery by `^## Epic` scans, hiding the epic from the retro sweep until manually located.

## Key Insights

1. A consolidated-scaffold delivery model is efficient but decouples stories from records — sprint-status and story files drift unless reconciliation is explicit.
2. Renames (`epic4` → `platform`) ripple through docs, scripts, and tests; stale-path references survive unless swept.
3. Heading consistency in planning artifacts is a discoverability requirement, not cosmetics.

## Action Items

1. **Reconcile Epic 4 story records** — 4-3..4-9 have no story files (delivered via the consolidated scaffold, paths renamed `epic4` → `platform`); 4-2 has unchecked tasks despite implementation existing. Backfill a minimal record per story or annotate the consolidated-scaffold delivery model, fix the stale paths in `epic-4-offline-gitops-scaffold.md`, and clear the 4-2 task checklist.
   Owner: John | Status: open | Category: documentation
2. **Normalize the Epic 4 heading** in epics.md from `### Epic 4` to `## Epic 4` (h2), matching every other epic, so machine-driven discovery and retro sweeps find it.
   Owner: Winston | Status: open | Category: documentation

## Team Agreements

- Carried forward: parity guards for shared config; deferred findings get named resolvers.

## Readiness Assessment

- **Testing & Quality:** Green (validation tests pass).
- **Deployment:** GitOps tree builds; not applied to a live cluster.
- **Stakeholder Acceptance:** Confirmed by Master (Project Lead).
- **Technical Health:** Sound; 4-3..4-10 record gap tracked as action item 1.

## Commitments & Next Steps

1. Execute Epic 4 action items 1–2.
2. Continue retrospective sweep: Epics 3–1.
