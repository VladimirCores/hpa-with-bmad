# Epic 9 Retrospective — AI Agent Engine (v2 - deferred)

- **Date:** 2026-08-11
- **Epic:** 9 — AI Agent Engine (v2 - deferred)
- **Stories:** 9-1 `expose-platform-capabilities-as-mcp-tools`, 9-2 `enable-agent-to-agent-communication` (done); 9-3 `provide-full-llm-decision-support-engine` (defined in epics.md, **not implemented / deferred**)
- **Status:** Partial — 2/2 implemented stories done; epic marked `done` in sprint-status though 9-3 was never built
- **Participants:** Amelia (Developer, facilitating), John (Product Manager), Winston (System Architect), Murat (Test Architect), Master (Project Lead)
- **Previous retrospective:** None (first recorded retrospective for this epic)
- **Next epic:** Epic 8 (next retro in the 10→1 sweep)

## Epic Summary & Metrics

- **Completion:** 2/3 planned stories; 2/2 implemented stories (100% of shipped scope)
- **Scope:** MCP-compatible tool definitions (`MCPToolRegistry` contract) and authenticated agent-to-agent messaging (`AgentCommunication` contract), each as a `gitops/agent-engine/` component with install script, step wrapper, and validation test
- **Constraint honored:** offline/GitOps-safe — no internet, no live cluster

## Successes

1. **Repeatable scaffolding.** 9-1 and 9-2 follow the identical contract shape — base manifest, dev overlay, `install-*-dev.py` with `--check`/`--dry-run`/`--apply`, `scripts/steps/` wrapper, validation test. The component pattern from Epics 1–8 carried through with zero novelty.
2. **Domain invariants gated.** A2A tests gate on the authenticated channel (no unauthorized impersonation); MCP tests gate on security-policy validation of invocations — plus the universal offline / non-zero-exit contract.

## Challenges & Growth Areas

1. **9-3 deferred by design.** The headline "full LLM decision-support engine" story was never implemented — the epic is scoped as "v2 - deferred". MCP tools + A2A messaging ship, but the reasoning engine does not exist. Explicit scope decision, not an accident — but it leaves epic-9 `done` with an unimplemented story on the books.
2. **Declarative only.** Manifests exist and build; nothing has been applied to a live cluster (shared caveat with Epic 10).

## Key Insights

1. The component / install-script / test skeleton is a proven, repeatable delivery pattern across all 10 epics.
2. Partial epics are acceptable when the deferral is explicit and recorded — the risk is an epic marked `done` with unimplemented stories left to discover later.

## Action Items

1. **Clarify story 9-3 status in epics.md** — mark `provide-full-llm-decision-support-engine` explicitly as deferred/not-implemented (epic currently reads `done` with an unimplemented story).
   Owner: John | Status: open | Category: process/documentation
2. **Record agent-engine parity guard** — `agent-engine` follows the same kustomize↔installer pattern flagged in Epic 10; apply the parity-guard agreement to any shared config.
   Owner: Winston | Status: open | Category: technical

## Team Agreements

- Carried forward from Epic 10: parity guards for any config consumed by both kustomize and installer scripts; deferred findings get named resolvers in `deferred-work.md`.

## Readiness Assessment

- **Testing & Quality:** Green (validation tests pass).
- **Deployment:** GitOps tree builds; not applied to a live cluster.
- **Stakeholder Acceptance:** Confirmed by Master (Project Lead).
- **Technical Health:** Sound; 9-3 gap tracked as a documentation/scope item.

## Commitments & Next Steps

1. Execute Epic 9 action items 1–2.
2. Continue retrospective sweep: Epic 8 next, then 7–1.
