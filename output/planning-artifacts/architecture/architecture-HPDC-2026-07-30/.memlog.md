---
scope: Enterprise GitOps Platform for IoT telemetry, alert management, and GitOps deployment
purpose: Architecture spine for HPDC — invariants for epics and stories
altitude: feature
updated: 2026-08-04T12:18
---

- (event) Architecture session started for HPDC. PRD has 48 FRs, 5 UJs. Clean-sheet, v1 MVP on local dev (talosctl QEMU). Altitude: feature (keeps epics consistent).
- (direction) User wants all auxiliary outputs: C4, interactive HTML deck, work-split view, ADR decision log, full solution design doc.
- (decision) Compute layer is serverless-first: KNative (scale-to-zero) + Restate for SAGA/stateful workflows, SpinKube WASM for stateless transforms, Pulsar Functions for stream processing. No always-on microservices. ADOPTS from PRD.
- (decision) All API routes (internal and external) defined in OpenAPI/Swagger specs stored in Git. ADOPTS from PRD FR-39.
- (direction) Paradigm proposed: Gateway-Mediated Domain Segregation — Envoy Gateway routes as domain boundaries, each with its own internal pattern. Event-mesh (Pulsar+Kafka) as cross-cutting fabric. Serverless compute layer.
- (decision) Pulsar payload format: Protobuf. ADOPTS from user.
- (decision) Spin function languages: Rust (primary), JS and Go (secondary). ADOPTS from user.
- (decision) Talos version: latest from 1.13.7. ADOPTS from user.
- (decision) Talos version: latest from 1.13.7. ADOPTS from user.
- (decision) Paradigm (ADOPTED): Gateway-Mediated Domain Segregation — Envoy routes as hard domain boundaries, each with its own internal pattern. Event-mesh (Pulsar+Kafka) as cross-cutting fabric. Serverless compute (KNative+SpinKube+Pulsar Functions).
- (decision) Repository strategy: monorepo with Kustomize overlays for GitOps delivery via Kargo+ArgoCD. ADOPTS from user.
- (decision) Environment stages for v1: dev + production. ADOPTS from user.
- (decision) Database ownership: CouchDB=CRM/ERP/docs, YugabyteDB=transactions/analytics/stats/reports/finance, ArcadeDB=graph, ClickHouse=telemetry, KeyDB=cache/pubsub/state, PostgreSQL=Casdoor+Casbin auth. All serverless functions can access all DBs. ADOPTS from user.
- (version) Pulsar 4.2.3, Talos 1.13.7, Cilium 1.19.6, SpinKube shim v0.25.1/Spin v4.0.1/Operator v0.6.1, Kargo v1.11.0, Rook-Ceph v1.20.3 (Ceph v20.2.1), YugabyteDB v2026.1.0.1, VictoriaMetrics v1.148.0, CouchDB 3.5.2, ArcadeDB 26.7.3, ClickHouse 26.7.1, Envoy Gateway v1.8.3.
- (decision) Source tree restructured: services/functions/{knative,spin,pulsar}/ for all serverless functions; services/ root for any future microservices. ADOPTS from user review.
- (decision) Casbin ext_authz: Go gRPC server called by Envoy Gateway SecurityPolicy.extAuth. Pattern validated from with-gsd reference. ADOPTS from PRD + reference.
- (decision) SpinApp CRD (core.spinoperator.dev/v1alpha1) as the deployment unit for Spin WASM functions. Validated from with-gsd reference.
- (decision) Source tree: services/ renamed to backend/, SPA frontend added as frontend/ (shipped from CDN). ADOPTS from user review.
- (direction) Testing strategy needed: unit, integration, and e2e tests covering deployment, serverless functions, and route exposure. Add as spine section.
- (decision) AD-12 — Observability: structured JSON logs, OpenTelemetry auto-instrumentation, VictoriaMetrics cluster, Hubble network observability, retention policies (7d raw / 30d agg / 1y monthly). ADOPTS from reviewer gate F-1.
- (decision) AD-13 — Centralized secrets management: Infisical K8s Operator CSI Driver injection, auto-rotation 90d, per-function service accounts. ADOPTS from reviewer gate F-3.
- (decision) YugabyteDB version corrected: 2025.2 LTS → 2026.1.0.1 (from memlog).
- (decision) Ceph version corrected: v20.2.1 → v20.2.2 (Rook v1.20.3 bundles newer). SpinKube org noted: spinframework.
- (decision) Ceph version corrected: v20.2.1 → v20.2.2 (Rook v1.20.3 bundles newer). SpinKube org noted: spinframework.
- (decision) AD-6 confirmed: universal R/W access for MVP. Shape conventions enforced via code review, not runtime.
- (decision) CDC loop prevention: both origin field + idempotency_key added to Protobuf CommonEnvelope (AD-5). Origin checked before write-back; idempotency_key dedup set in KeyDB (TTL 5m).
- (decision) Auth model conflict resolution: DENY wins. If any model (RBAC/ReBAC/ABAC) denies, decision is DENY.
- (event) spine finalized. 13 ADs, 48 FRs mapped, reviewer gate passed, all auxiliary outputs generated (C4 diagrams, interactive HTML deck, work-split view, ADR log, solution design doc).
- (decision) AD-14 Internal function authorization: inter-domain communication remains event-mesh or database change feeds; same-domain KNative/SpinApp HTTP calls require Cilium SPIFFE/SPIRE identity, callee-side caller allow-list authorization, no public function endpoints, and context-based timeouts; mTLS authenticates transport but does not substitute for authorization.
- (decision) AD-15 Per-function database access scoping: every function declares minimum required database access in a Git-owned policy file; access is enforced with per-function Infisical credentials; each function may read/write only approved databases, collections, tables, and KeyDB prefixes; CI validates manifests against the policy matrix before Kargo promotion.
- (decision) AD-16 Change-feed loop detection: all CouchDB _changes and YugabyteDB CDC events that trigger KNative or Restate workflows must carry event_id, origin, idempotency_key, target domain, and mutation direction; sinks must ignore self-origin events, enforce idempotency, and prevent recursive CDC/_changes loops.
- (decision) AD-17 Workflow orchestrator boundary: Restate owns stateful SAGA/workflow state, durable business processes, idempotent state transitions, and change-feed-driven workflows; Argo Workflows owns CI/CD, build/test/scan/deploy automation, batch orchestration, and one-shot offline GitOps jobs. A business workflow must not be implemented in both systems.
- (direction) Clarified AD-17 source ownership: business workflow implementation code belongs under backend/ as serverless functions or SAGA modules; platform/workflows/ contains only orchestrator policy matrices, allowed transition graphs, and ownership rules.
- (decision) AD-18 GitOps component upgrade enforcement: all platform component upgrades must be declared as GitOps changes, promoted through Kargo Freight and Argo CD, include version/rollback target/compatibility/sync-wave/validation evidence, and reject in-place Helm upgrades, direct kubectl changes, and manual image tag swaps.
- (decision) AD-19 Version pinning and provenance enforcement: GitOps manifests must pin platform components by immutable digest or exact version, not latest or mutable tags; Kargo Freight may promote only Harbor images/configs with scan, SBOM, and signing evidence; stack-table mutable versions need exact pins or approved exceptions with owner, expiry, and rollback target.
